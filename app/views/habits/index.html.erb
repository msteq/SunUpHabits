<div class="min-h-screen bg-[#f8f2ec] p-8">
  <div class="max-w-2xl mx-auto bg-white rounded shadow p-6">
    <h2 class="text-2xl font-bold text-black mb-6 text-center">マイ習慣</h2>

    <% if @habits.any? %>
    <ul>
      <% @habits.each do |habit| %>
      <li class="mb-4 p-4 border border-gray-300 rounded">
        <h3 class="text-xl font-semibold"><%= habit.title %></h3>
        <p class="text-gray-700"><%= habit.goal %></p>
        <p class="text-gray-500">開始日: <%= habit.start_date.strftime('%Y-%m-%d') %></p>
      </li>
      <% end %>
    </ul>
    <% else %>
    <p class="text-center text-gray-700">まだ習慣が登録されていません。</p>
    <% end %>

    <div class="mt-6 text-center">
      <%= link_to '新規習慣登録', new_habit_path, class: "btn bg-[#FFA500] text-black text-lg hover:bg-[#C28742] px-4 py-2 rounded" %>
    </div>
  </div>
</div>